#!/bin/bash

echo "--- Pyrightを実行します ---"

uv run pyright

EXIT_CODE=$?

if [ $EXIT_CODE -ne 0 ]; then
  echo "---"
  echo "❌ Pyrightでエラーを検知しました"
  echo "uv run pyright でエラーを確認の上、修正して下さい"
  echo "---"
  exit 1
fi

echo "✅ Pyrightチェックをパスしました。コミットします"
exit 0