# YUGIOH-FTS-VSS UI Design

## 方針

遊戯王OCGカード検索アプリのUIをリッチ化する。
Streamlit の制約内で最大限の視覚的改善を行い、
カード検索体験を向上させる。

## テーマ

- **固定**: ライト（Streamlit デフォルト）
- テーマ切り替えは実装しない（CSS injection での切り替えは Streamlit の内部構造に依存し保守困難なため）

## レイアウト

- サイドバーなし・すべて main に集約
- 検索フォーム → フィルター行 → カードグリッドの縦一本導線

## コンポーネント設計

### 検索バー
- テキスト入力 + 件数スライダー + 実行ボタン
- レイアウト比: 3:1:1

### フィルター行（検索後に出現）
- 検索結果に存在するカード種・属性の値のみを選択肢として動的生成
- カード種 / 属性 を横並び2列の multiselect で表示
- フィルター変更は再検索不要で即時反映

### カードグリッド
- 1行3枚並び（wide レイアウトを活用）
- 各カードは `st.container(border=True)` でボックス化
- カード内レイアウト:
  1. **カード名** (太字)
  2. **カード種バッジ** + **属性バッジ** (横並び2列)
  3. **ステータス行**: 種族・Lv・ATK・DEF・スケール・リンク（"ー"はスキップ）
  4. **テキスト** (expanderで折りたたみ)

## スコープ外

### カード画像表示
- YGOPRODeck は CDN からの頻繁なロードを禁止。自前ホストが必要
- parquet への画像バイナリ埋め込みは `update_cards.py` 側の改修が必要なため、このアプリ単体では完結しない
- 将来対応する場合は `cards_small` サイズ（約15KB/枚）を parquet に埋め込み、`st.image(card["image"])` で表示する方針

## バッジカラー規則

カード種:
- 魔法 → green
- 罠 → red
- 通常モンスター → primary（青）
- 効果モンスター → yellow
- 融合 → violet
- シンクロ → orange
- エクシーズ → gray
- リンク → blue
- 儀式 → green

属性:
- 光 → yellow
- 闇 → violet
- 炎 → red
- 水 → blue
- 風 → green
- 地 → orange
- 神 → gray
